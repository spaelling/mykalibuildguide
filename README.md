# mykalibuildguide
Writing down steps to build my very first Kali Linux box that I will probably brick at some point

Generally follow steps in 0, then 1, etc.

After first booting up run in a terminal as root.

```
sudo apt-get update
sudo apt-get upgrade
```

Second command will take a while. Then reboot.